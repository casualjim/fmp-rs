/target
/node_modules

/bun.lock
/Cargo.lock
.env
.env.*
!.env.example
.mcp.json
.mcp.json.bak
mcp.json
.claude/settings.local.json
.*.vars
*.log
.crush
crush.json
CRUSH.md
compose.override.yml
QWEN.md
qwen-*.sh
warp-*.md

# Functions use the .func directory for local runtime data which should
# generally not be tracked in source control. To instruct the system to track
# .func in source control, comment the following line (prefix it with '# ').
/.func
qwen.sh
/memory.db
.sctl.toml
/.vscode

# Paid bulk fixtures must never be committed
/tests/fixtures/bulk_*.csv
/tests/fixtures/bulk_*.csv.xz
/tests/fixtures/profile.part.*
